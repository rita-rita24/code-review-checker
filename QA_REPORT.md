# 内部品質QAレポート

確認日: 2026-09-23（以前の検証記録も保持）

## 最新の追加改善（2026-09-23・既存ルールと操作の追加点検）

開始時点の149件はすべて成功していました。回帰テストを13件追加し、開始時点のHTMLに適用すると12件が失敗、1件が成功しました。失敗する条件を修正し、正常な対照ケースも維持しています。既存の未コミットの変更を保持し、画面・静的文言・ボタン・CSSは増やしていません。

| GOODになった点 | 変更内容 |
| --- | --- |
| JavaScriptの誤検出を削減 | `globalThis.Number` などの標準APIの置換・削除と、`document.createElement`・`console.log` の書き換えを追跡。分割代入やループによる代入も扱い、同名のローカル引数や無関係なプロパティの変更とは区別 |
| プロパティの判定を統一 | 固定文字列のテンプレートによるプロパティ名にも既存ルールを適用。`#value`・`#log` などのプライベートメンバーをDOMやconsoleの公開APIと混同する誤判定を修正 |
| Javaの例外処理の見逃しを削減 | 注釈・`final`・完全修飾名を含むcatchを構文木で検査。注釈の引数に括弧があっても空のcatchを検出し、独自の `Exception` 型は標準の広いcatchと混同しない |
| Javaのログ検査を改善 | 改行・空白・コメントを挟んだ呼び出しを検出し、メソッド名のある行へ移動。独自の `System` クラス、同名の変数、別オブジェクトのメンバーは標準出力と混同しない |
| Javaの文字列比較の見逃しを削減 | 片方がnullの条件式でも文字列・配列の参照型を維持し、`var` や配列要素の比較を検査。null確認や配列全体の同一性比較は従来どおり許可 |
| 結果の読み上げを改善 | 既存の結果ボタンに指摘本文を `aria-describedby` で関連付け、移動先の入力欄を `aria-controls` で指定。表示要素や説明文の追加はなし |

### 検証記録

- 追加13件の回帰テスト: 成功。作業前のHTMLでは12件失敗することを確認。
- 最終コードの `npm test`: **161件成功 / 162件**。不正入力500パターン・状態管理・追加13件を含む機能検証は成功。性能テスト1件が、大量Java入力の解析時間上限で失敗しました。スキップはありません。以下の単独性能検証の成功と区別して記録しています。
- 生成HTMLとソースの一致、ソース3ファイルの構文検査、`git diff --check`: 成功。
- `npm run test:browser`: **3エンジン × 32シナリオ成功**。計96シナリオ、親テストを含むNodeの集計では99件成功。外部HTTP通信・ページ例外はいずれも0件。
- 初期画面はChromium・Firefox・WebKitすべてで作業前とピクセル単位で一致（1280×900、ライトモード）。静的HTML・CSS・文言・ボタンも既存のSHA-256検査に合格。
- ブラウザーテストを各29件から32件へ拡充。新しい判定の実Worker経由の動作、正しい行への移動、Enterでの操作、Shift+Tabで入力欄から抜けられることを確認。
- Chromiumではアクセシビリティツリーから実際のボタンの説明に指摘本文が含まれることも検査。実機のスクリーンリーダー全般の認証ではありません。
- WebKitの既定設定ではShift+Tabがボタンを飛ばしてブラウザー側に移ります。作業前後のHTMLで同じ挙動を確認し、特定のボタンへ移るというテストの前提を修正しました。
- CPU負荷が高い全体検証で、大量Java入力が既存の解析時間上限に達しました。端末のロードアベレージは43.46でした。製品の解析上限・性能テストの合格条件は変更せず、作業前後を順番に単独測定したところ、両方が成功しました。約31.9万文字のJavaの測定値は作業前1,898 ms・修正後1,842 msで、全性能シナリオも既存の各入力5秒・全体15秒の条件内でした。機器負荷の影響を含む測定値であり、速度向上の保証ではありません。
- 依存パッケージの変更はありません。`npm audit` は今回再実行していません。過去の監査記録は以下に保持しています。

動的なプロパティ名や別名経由のAPI変更、外部ファイルの型、Javaの完全なオーバーロード解決などは従来どおり対象外です。

## 前回の追加改善（2026-09-23・UIを増やさない点検）

開始時点の133件はすべて成功していました。今回追加した16件を開始時点のHTMLに適用すると、すべて失敗しました。解析・修正例の13件とテーマ状態の3件を修正しています。新しい画面、静的文言、ボタン、CSSは追加していません。以前からの未コミットの変更も保持しています。

| GOODになった点 | 変更内容 |
| --- | --- |
| JavaScriptの見逃しを削減 | `input.value === (choice = 1)`、`input.value === ++choice` など、代入式・更新式・複合代入の結果型を判定。NumberとBigIntのどちらにもなり得る数値結果を文字列と区別し、論理代入の不明な結果は断定しない |
| JavaScriptの誤検出を削減 | `Number`・`document`・`input`・`console` など、宣言のないグローバル識別子に代入された場合は組み込みAPIやDOMという推定を無効化。関数内の同名の引数への代入とは区別 |
| Javaの型注釈に対応 | `java.lang.@Nullable String` や `String @Nullable []` を元の型として扱う。文字列や入れ子の注釈を含む引数も構文木で区別し、戻り値・キャスト・継承・生成式の判定を維持 |
| Javaの配列と呼び出しを改善 | 生成した配列とその要素を区別。添字や配列サイズの式の中の配列を数えず、実際の次元数を追跡。無修飾の呼び出しでは外側のクラスのメソッドを解決し、`this` や同名のメソッドでは外側へ型を漏らさない |
| 修正例の信頼性を改善 | 160文字に省略した行は、構文上読み取れても置換コードを生成せず既存の説明を使用。同じ行のコメント付き制御文を飛ばし、実際にスペースが不足している箇所の修正例を作る |
| 既存の書式ルールを徹底 | `do...while` の末尾の `while` にもスペースの検査を適用し、正しい行番号と修正例を返す。キーワードと括弧の間のコメントは保持 |
| テーマの操作を改善 | ページを開いたままOSの配色変更に追従し、手動選択後はその設定を維持。保存制限時の選択もメモリー内で保持。同じHTMLを開いた別ページとテーマを同期し、保存設定の削除ではOS設定へ戻る |
| 検証の再現性を改善 | CPU負荷を伴う単体テストの同時起動を避けるよう実行設定を変更。ブラウザーテストと性能測定も順番に実行する手順を明記。不正入力500件の正しさを確認するテストは、負荷のある環境でも全件を検査できるよう待ち時間を60秒に変更 |

### 今回の検証

- `npm test`: **149件成功**（開始時点133件＋追加16件）。不正入力500パターン、性能テスト、状態管理、静的UIの一致を含む最終実行で失敗・スキップなし。
- 静的HTML・CSS・文言・ボタンは開始時点とSHA-256で一致。初期画面はChromium・Firefox・WebKitすべてでピクセル単位で一致（1280×900、ライトモード）。
- `npm run test:browser`: **3エンジン × 29シナリオ成功**。計87シナリオ、親テストを含むNodeの集計では90件成功。外部HTTP通信とページ例外はいずれも0件。
- 最終ブラウザーテストには、実Worker経由の新しい判定、長い行の修正例、OSテーマ変更、手動選択の保持、保存制限、ページ間同期を追加。
- 追加16件を開始時点のHTMLに適用し、16件とも失敗することを確認。正常な対照ケースも各テスト内で検査。
- `npm run check:parser`、JavaScriptソースの構文検査、`git diff --check`: 成功。配布HTMLとソースの生成一致を確認。リモートCIは実行していません。
- 高いCPU負荷の下で、正常なJavaの負荷入力が既存の解析時間上限に達し、不正入力テストも15秒の待ち時間を超過。開始時点のHTMLでも不正入力テストの待ち時間超過を確認。製品の解析上限と、性能テストの各入力5秒・全体15秒の条件は維持し、テスト同士のCPU競合を避ける実行順へ変更。不正入力テストの待ち時間だけ60秒に変更し、500件すべての判定を継続して確認。
- `npm audit` は、依存情報の外部送信への明示的な許可がないとして自動承認レビューに拒否されました。この回の脆弱性監査は未完了です。以下に残す過去の「0件」はこの回の監査結果ではありません。

既存の解析範囲と制限は引き続き適用されます。グローバルオブジェクトのプロパティ変更、外部ファイル、動的評価、Javaの完全なオーバーロード解決などを網羅する変更ではありません。

## 評価

改善前の状態を、有料提供に十分な品質とは評価できませんでした。24件の初期再現テストのうち18件が失敗し、誤検出・見逃し・行参照の不具合に加え、長い入力で解析が終了しない問題を確認しました。

今回、再現した不具合の修正、解析器の導入、状態管理と大量入力処理の改善、回帰テストとCIの整備まで行いました。既存ルールに基づくレビュー補助ツールとして、品質を判断できる検証結果が揃っています。「あらゆる不具合を検出する」「コンパイラーやセキュリティ診断を代替する」という品質保証はできません。有料提供時の対象は、ここで検証した命名・書式・限定的なコード品質チェックの範囲です。

## 改善内容

| 対象 | 修正した内部処理 |
| --- | --- |
| JavaScript解析 | Acornによる構文解析とacorn-looseによる復旧解析を導入。正規表現・文字列・テンプレートの式・複数行の文・関数宣言と関数式を区別 |
| JavaScriptの型とスコープ | 分割代入、引数、import、switch、クラスのstaticブロック、変数の再代入と再宣言を追跡。ローカルconsoleや数値を持つ通常オブジェクトの誤検出を抑制 |
| Java解析 | 構文木に基づき複数宣言、複数行の引数・戻り値、インターフェース定数、enum、ラムダ式、レコードを検査 |
| Javaの型とスコープ | フィールド、同一入力内の継承・戻り値、配列要素、括弧・キャストを追跡。入れ子・無名クラス、型パラメーター、独自のString型、別メソッドの変数を区別 |
| 比較 | null確認や配列の同一性比較をString比較と誤認しない。DOMの値と、型の分からない通常オブジェクトの値を区別。不明な加算結果を数値と断定しない |
| 書式 | 複数行の文の終端、括弧なしの制御文、入れ子のswitchなどを考慮。コメントやリテラル内の記号をコードの記号として扱わない |
| 行参照 | 実改行と文字列内の `\\n`、CRLF・CR、Unicode区切り文字を区別。比較演算子の実際の行を指摘し、クリックで該当行を選択 |
| 修正例 | 引用符変換で文字列の値を保持。対象の比較演算子・キーワード・ブロックだけを修正。無関係な文字列やオブジェクトを変更せず、切り詰めた断片から修正コードを捏造しない |
| 解析の失敗 | 不完全なコードでも独立した指摘を残す。解析失敗を「指摘なし」と表示しない。内部のcomplete/partial状態を管理 |
| 応答性 | 解析とカード生成をWeb Workerへ移行。結果を分割描画し、行番号のDOMを表示範囲に限定。過剰な正規表現処理・繰り返し走査を削減 |
| 状態と資源 | 入力更新、クリア、言語切替で古い処理と応答を無効化。クリア時は稼働中・待機中のワーカーとBlob URLを解放。ページ復帰とWorker禁止環境にも対応 |
| 編集操作 | 選択範囲へのTabでコードを失わない。ネイティブの取り消し履歴を利用。IME入力の確定まで解析を待つ |
| 保守性 | 編集用ソースを分離し、単一HTMLの生成を自動化。依存を固定し、ライセンスを同梱。テストと生成一致のCIを追加 |

画面のHTML・CSS・静的文言・ボタンは変更していません。元からあった未コミットの変更も保持しています。指摘される内容とコードの修正例は、判定の修正に応じて変わります。

## 追加点検での改善（2026-09-22）

既存の96件の回帰テストが通る状態から、境界条件の再現テストを追加して改善しました。

| 対象 | 追加で修正した処理 |
| --- | --- |
| 解析の停止と速度 | 入力途中の `switch` の例外を修正。共有する条件式の型推定をキャッシュし、約24行でも3秒を超えていた重複解析を解消。1,000段の参照を性能テストに追加。命名規則の正規表現・集合の繰り返し生成を削減 |
| JavaScriptの判定 | 括弧を挟む比較の演算子行、複数・改行されたクラス名、改行された関数名、引数の初期値と関数本体のスコープ、switch条件のスコープ、optional chaining・delete・カンマ式を修正 |
| Javaの判定 | 入れ子の添字・メソッドが返す配列・括弧を付けた配列の型、tryリソースの有効範囲、Boolean型の完全修飾名と同名の独自型、String生成後のメソッド呼び出しを区別 |
| Javaのパターン変数 | if・while・forの分岐、短絡評価、単純なreturn／throwガードから有効範囲を追跡。成立しない分岐に型を漏らさず、外側の同名フィールドを区別 |
| 修正例 | エスケープされた文字列からstrict modeを有効化しない。キーワード間のコメントを削除しない。関数宣言のセミコロンだけを対象にし、後続の関数式を変更しない |
| 操作と状態 | IME中の言語切替・Tab・クリア、先頭が空行の範囲へのTab、逆方向の選択を修正。古い行マーカーと解析データを入力更新時に解放。Workerの送信・受信エラーから復帰 |
| 検証の副作用 | 大量入力テストでOSのクリップボードを使わない方式に変更。画像などのクリップボード内容をテストが失わせることを防止 |

## 今回の追加改善（2026-09-23）

今回の開始時点では既存の121件が成功していました。追加した12件の解析テストを作業前のHTMLに適用すると、10件が失敗しました。2件は既存の正しい判定を維持するための対照テストです。再現した問題を修正し、UI・静的文言・ボタン・CSSを増やしていません。

| 対象 | 今回の変更 |
| --- | --- |
| JavaScriptの誤検出 | 分割代入や引数のデフォルト値を実際の型と断定しない。`with`の内部では動的な名前解決を考慮し、外側への代入の可能性も追跡 |
| JavaScriptの型推定 | BigInt変換を扱い、組み込み関数の一覧でプロトタイプ由来の名前を誤って参照しない。DOM要素の型推定を再利用し、1,000段の別名参照でも判定を維持 |
| Javaの誤検出と見逃し | `getText().length()`などの連続呼び出しを最初のメソッドの戻り値と混同しない。直接呼び出し・入れ子の引数・配列要素は引き続き検査 |
| Javaの式と入力途中の処理 | 代入先の宣言型、`instanceof`と比較の優先順位、Boolean型のビット演算を反映。入力途中でもクラスと同名のメソッドをコンストラクターと混同しない |
| 解析の継続性 | Javaパーサーの先読み回数と経過時間に上限を設け、未完の入れ子の型で処理が長時間続くことを防止。復旧用正規表現の繰り返し走査も削減し、打ち切り後に内部状態とトークン参照を解放 |
| 編集操作 | クリアにネイティブの取り消し履歴を利用。通常サイズではWebKitの入力と削除の連結も防止。選択範囲へのTabが文字列内のUnicode区切り文字にスペースを挿入する問題を修正 |
| 状態と描画 | 通常のページ復帰で復元された入力を再解析。指摘のない入力更新時には行マーカー用の配列生成と走査を削減。行番号の表示範囲を実際の行数内に制限 |
| テーマと検証 | 保存領域が使えない場合もOSのダークモード設定を反映。ブラウザーテストの全ページで例外・外部通信を監視し、Tabの取り消しテストから別シナリオの編集履歴を分離 |

Javaの解析上限は先読み時に確認します。初期化・字句解析・1回の先読み処理などをタイマーで強制終了するものではなく、厳密な応答時間保証ではありません。打ち切った場合は部分解析となり、「指摘なし」の成功表示を出しません。1万メソッドを含む正常なJava入力は完全解析されることも性能テストで確認しています。

クリア時の履歴の区切り直しは10万文字以下に限定しています。巨大入力では履歴の再生に伴うブラウザーの再レイアウトを避け、ブラウザー本来の取り消しのまとまり方に従います。編集APIを禁止した環境では入力とクリアの動作を保ちますが、取り消し履歴は保証できません。

### 今回の最終検証

- `npm test`: **133件成功**。追加12件の解析テスト、500パターンの不正入力、性能テスト、状態管理、静的UIの一致を含みます。
- `npm run test:browser`: **3エンジン × 25シナリオ成功**。親テストを含むNodeの集計では78件成功。
- 作業前に保存したHTMLと、Chromium・Firefox・WebKitそれぞれで初期画面のスクリーンショットがピクセル単位で一致。比較条件は1280×900・ライトモード。静的HTML・CSS・文言・ボタンのSHA-256も従来の値と一致。
- 4万行の解析中でもクリアが1.5秒以内に応答し、結果やワーカーが残らないことを確認。通常サイズのクリアは3エンジンで取り消し・やり直しと解析結果の追従を確認。
- 深い未完のJava型を含む約5万文字の入力は部分解析で終了し、その直後の正常なコードを完全解析。性能テスト中の測定例では約1.4秒。正常な1万メソッドのJava入力と1万宣言のJavaScript入力も完全解析。
- `npm run check:parser`: 成功。ソースと配布HTMLの生成一致を確認。
- `npm audit`: 確認時点の既知の依存脆弱性 **0件**。
- `git diff --check`: 成功。リモートCIはこの作業では実行していません。

## 前回の検証（2026-09-22）

- `npm ci --ignore-scripts`: 成功。ロックファイルから依存を再インストール。
- `npm run check:parser`: 成功。アプリと解析器を含む配布HTMLの生成一致を確認。
- `npm test`: **121件成功**（追加25件）。うち1件で、不正・未完・混在した入力を固定シードで500パターン生成し、例外・行範囲・順序・重複を検査。生成パターンにもswitch・パターン変数・リソース・入れ子の添字を追加。
- `npm run test:browser`: **3エンジン × 20シナリオ成功**。Nodeの集計では親テストを含め63件成功。
- 改善前と改善後の初期画面が、3エンジンすべてでピクセル単位で一致。静的HTML・CSS・文言・ボタンはSHA-256でも一致を確認。
- 1,000行分の結果が最後まで描画され、1,000行目への移動が正しいことを確認。行番号のDOMは200要素未満に維持。
- 約91万文字・4万行の入力で、解析中のクリアが1.5秒以内に応答し、古い結果が復活せず、ワーカーが残らないことを確認。
- 入力コードが実行されず、HTML要素として挿入されず、外部HTTP通信が発生しないことを確認。
- `npm audit`: 検査時点のロック済み依存で既知の脆弱性 **0件**。
- `git diff --check`: 成功。

ブラウザーで確認した20項目は、初期起動、言語切替、該当行選択、入力コードの非実行、テンプレートと正規表現、連続操作、選択範囲のTab、Tabの取り消し、先頭空行と逆方向の選択、括弧を含む比較の行選択、不完全入力、大量の結果描画、解析中断、IME、IME中の言語切替とクリア、分割描画中の入力更新、Worker送信失敗、Worker禁止環境、ページ復帰、ストレージ制限時のテーマ切替です。

WebKitのオフラインエミュレーションはローカルファイルとBlob Workerまで読み込めなくするため、WebKitではHTTP通信を遮断して検証しました。大量入力は3エンジンとも値設定とinputイベントで解析・中断を検証しています。自動化APIのinsertTextで4万行を挿入するとアプリの処理がなくても停止するChromiumの経路を避け、OSのクリップボードにはアクセスしません。この負荷テストは実際の貼り付け操作そのものを検証するものではありません。

## 性能測定

Node.js 22.22.2・ローカルmacOSで、性能テストを単独実行した測定例です。隔離ワーカー内で解析のみを測定し、描画時間は含みません。初回Javaの測定には解析器の初期化も含みます。

| 入力 | 解析時間 |
| --- | ---: |
| Java: 30,000個の空白と識別子 | 約132 ms |
| Java: 100,000文字の識別子 | 約88 ms |
| Java: 10,000メソッド、約319,000文字 | 約239 ms |
| JavaScript: 10,000宣言、約279,000文字 | 約207 ms |
| JavaScript: 100,000文字の文字列 | 約7 ms |

性能テストは各入力5秒以内・全体15秒以内を条件としています。機器・負荷・ブラウザーによる差があるため、上表は製品の応答時間保証ではありません。

追加の性能テストでは、約45,700文字の共有する条件式1,000段について、数値型のケースが約57 ms、型が不明なケースが約37 msで完了した測定例があります。既知の型だけでなく、不明な型でも重複解析が増え続けないことを検証しています。

## 検証の範囲

- 単一の貼り付けコードに対する静的レビューです。外部依存や別ファイルの型、実行時の値、コンパイルや実際の振る舞いは検証しません。Javaの完全なオーバーロード解決やフロー解析も対象外です。
- JSX・TypeScriptなどの非対応構文や構文エラーを説明する画面は追加していません。不完全解析では成功表示を抑制し、分かる範囲の指摘を返します。
- 命名・インデント・DOMコード断片などの規約にはプロジェクト固有の前提が残ります。すべてのチームに適合するルールではありません。
- Workerを禁止した環境でも動作しますが、この場合の巨大入力はメインスレッドを使用します。任意サイズの入力に対するメモリー・応答時間の保証はありません。
- ローカルのChromium・Firefox・WebKitで検証しています。全OS・全ブラウザーバージョン・実機の日本語IME・モバイルを網羅した認証ではありません。GitHub Actionsの設定は追加済みですが、リモートCIはこの作業では実行していません。
